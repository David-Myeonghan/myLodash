type collectionType = unknown;
type predicateType = unknown;

export default function find(collection: collectionType, predicate: predicateType ) {
    console.log(predicate);

    return collection;
}